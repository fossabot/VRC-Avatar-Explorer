using Avalonia;

namespace AvatarExplorer.UI.Models;

internal static class AvaloniaVectorUtils
{
    internal static readonly Vector MaxValue = new(double.MaxValue, double.MaxValue);
    internal static readonly Vector MinValue = new(double.MinValue, double.MinValue);
}
